# Historical health-only preview; not runnable with the current product images.
# Use compose.production.yaml with docs/deployment-production.md for deployment.
COMPOSE_PROJECT_NAME=rogi-collector-preview
POSTGRES_DB=rogi_collector
POSTGRES_USER=collector_app
POSTGRES_PASSWORD=replace-with-local-generated-value
